Act as an elite Software Engineer and DSA expert. Generate a structured markdown note for the following DSA problem.

Problem Name: {problem_name}
User Note/Mistake: {mistake}

Follow these STRICT formatting rules:

1. **Header:**
   - Start with '# {problem_name}'.
   - Use '##' for main headers.

2. **Difficulty:**
   - Header: `## Difficulty`
   - Classify as Easy, Medium, or Hard based on interview standards.
   - Use both a header and a tag: #easy, #medium, or #hard.

3. **Pattern:**
   - Header: `## Pattern`
   - Define the algorithmic pattern (e.g., Two Pointers, Sliding Window, Greedy, Backtracking).

4. **Key Idea:**
   - Header: `## Key Idea`
   - Summarize the core intuition in 1-2 sentences.

5. **Approach:**
   - Header: `## Approach`
   - **Brute Force:** Describe the naive approach and why it's inefficient.
   - **Optimal:** Explain the most efficient logic clearly, step-by-step.

6. **Code:**
   - Header: `## Code`
   - Provide clean, Pythonic code.
   - Include inline comments explaining complex transitions.

7. **Dry Run:**
   - Header: `## Dry Run`
   - Create a Markdown table. Trace a non-trivial sample input step-by-step (at least 3-4 steps), showing variable changes (e.g., `i`, `j`, `current_sum`, `ans`).

8. **Edge Cases:**
   - Header: `## Edge Cases`
   - List 3-4 specific scenarios (e.g., empty array, $n=1$, all identical elements, negative integers).

9. **Mistakes:**
   - Header: `## Mistakes`
   - Use an Obsidian Callout (> [!CAUTION] Mistakes).
   - List logical "gotchas" or syntax errors.
   - Address the user's specific note: {mistake}

10. **Complexity:**
    - Header: `## Complexity`
    - Use LaTeX for all math: $O(n)$ or $O(n \log n)$.
    - Provide a 1-sentence justification for both Time and Space complexity.

11. **Metadata & Placement Tags:**
    - Header: `## Metadata & Placement Tags`
    - **PLACEMENT: This section MUST be at the very bottom of the note.**
    - Identify common companies that ask this question.
    - Add a "Confidence Level" placeholder: [ ] Low [ ] Mid [ ] High.
    - Include 2-4 concept links using PascalCase inside [[ ]] and a hashtag, e.g., #prefixsum [[Prefix Sum]], #slidingwindow [[Sliding Window]], #hashmap [[HashMap]].

12. **Output ONLY the Markdown content.** No "Here is your note" or "Sure, I can help."
13. **Include a horizontal separator '---' between main sections.**
14. **Ensure the total output is at least 250 words.**

# {problem_name}
---
